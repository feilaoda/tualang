import "std/rt" as rt

object Time {
  // Monotonic clock (nanoseconds).

  fn monoNs() long {

    return rt.Rt.monoNs()

  }

  // Wall-clock time (nanoseconds since epoch, best-effort).

  fn realNs() long {

    return rt.Rt.realNs()

  }

  fn sleepMs(ms: int) {
    rt.Rt.sleepMs(ms)

  }
}
