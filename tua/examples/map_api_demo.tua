let m: map = { "a": 1, 2: 3 }

assert(m.hasKey("a"))
assert(!m.hasKey("missing"))

let v:any = m["a"].unwrap()
assert((long)v == 1L)

let o2 = m["missing"]
assert(o2.isNone())

assert(len(m) == 2)
assert(m.len() == 2)
