// tuac: --profile script
import "std/bytes" as bstd

fn main() {
  let b: bytes = bstd.Bytes.fromString("abc")
  let a: bytes[] = { b }

  // Self assignment should be a no-op for RC bookkeeping.
  a[0] = a[0]

  assert((int)a[0].get(0L) == 97)
  assert((int)a[0].get(1L) == 98)
}
