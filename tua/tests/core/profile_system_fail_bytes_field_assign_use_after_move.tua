// tuac: --profile system
import "std/bytes" as bstd

struct Holder {
  b: bytes
}

fn main() {
  let src: bytes = bstd.Bytes.fromString("abc")
  let h = Holder{ b: bstd.Bytes.new(0L) }

  h.b = src
  println((int)src.get(0L))
}
