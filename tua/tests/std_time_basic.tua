import "std/time" as time

fn main() {
 
 
  let a = time.Time.monoNs()
    time.Time.sleepMs(1)
 
 
  let b = time.Time.monoNs()
    assert(b >= a)

 
 
  let r = time.Time.realNs()
    assert(r >= 0)
}

