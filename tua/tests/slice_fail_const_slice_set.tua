import "std/bytes" as bstd


// const slice is readonly

fn main() {
    let b: bytes = bstd.Bytes.fromString("abc")
    const s: Slice<byte> = b.slice(1L, 2L)
    let e = s.set(0L, (byte)120)
    println(e)
}
