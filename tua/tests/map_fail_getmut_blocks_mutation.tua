// m is mutably borrowed by `o`

// keep `o` alive past the mutation so the borrow can't end early (NLL)

fn main() {

  let m: map = {}
  m["a"] = 1

  let o = m.getMut("a")
  m.delete("a")
  assert(o.isSome())
}
