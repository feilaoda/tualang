fn main() {
 
 
  let a:int[3] = {1, 2, 3}
 
 
  let sum:int = 0
 
 
  let sumIdx:int = 0
 
 
  for v,i in a {
        sum = sum + v
        sumIdx = sumIdx + i
 
   }
    assert(sum == 6)
    assert(sumIdx == 3)
}
