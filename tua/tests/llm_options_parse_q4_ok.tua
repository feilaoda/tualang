// tuac: -L build/clib -l tuallm

import "../packages/llm/options" as o

fn main(args: string[]) -> int {
    let argv: string[] = {"x", "--w-q4"}
    let e, opt = o.LlmArgs.parseArgv(argv)
    assert(e == 0)
    assert(opt.weightsDtype == 3)
    return 0
}
