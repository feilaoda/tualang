

// Typed map index read for move-only values is forbidden (must use get/getMut).

fn main() {
    let m: map<string, map<string, int>> = {}
    m["a"] = { "k": 1 }
    let v = m["a"].unwrap()
    println(v.len())
}
