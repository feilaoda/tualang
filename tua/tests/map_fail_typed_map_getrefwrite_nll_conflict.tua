struct S {
  a: int
  b: int
}

let m: map<string, S> = { "x": S{ a: 1, b: 2 } }
let r = m.getRefWrite("x").unwrap()
r.a = 10

let r1 = m.getRef("x").unwrap() // error: r is still used later, so the write-borrow is still active
assert(r1.a == 10)

r.b = 20
