

// m is borrowed by `o` (Option<Ref<...>>)

// keep `o` alive past the mutation so the borrow can't end early (NLL)

fn main() {
    let m: map = {}
    m["a"] = 1
    let o = m.get("a")
    m["b"] = 2
    assert(o.isSome())
}
