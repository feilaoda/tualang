// tuac: -L build/clib -l tuallm

import "llm/options" as o

fn main(args: string[]) -> int {

  let argv: string[] = {"x", "--profile"}

  let opt, e = o.LlmArgs.parseArgv(argv)
  assert(e == 0)
  assert(opt.profile == 1)

  return 0
}
