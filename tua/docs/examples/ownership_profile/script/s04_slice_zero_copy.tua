// draft-profile: script
// expect-target: pass

fn checksum(s: Slice<byte>) long {
  let sum: long = 0
  for i = 0; i < s.len(); i++ {
    sum = sum + s.get(i)
  }
  return sum
}

fn main() int {
  let b = bytes(16)
  let s = b.slice(0, 16)
  println(checksum(s))
  return 0
}
