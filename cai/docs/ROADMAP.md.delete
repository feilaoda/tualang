# CAI 实现路线图（Roadmap）

Status: Draft

本文件是实现计划（不是语言规范）。目标：优先实现 **Tensor 计算** 的“完整能力闭环”，避免为特定情况写死导致未来 GPU/新 dtype/新算子返工。

约定：
- 本文用 **M0/M1/…** 表示实现里程碑（对应你说的“v0/后续阶段”），不是语言版本号。
- 优先级统一为：`P0` 必须、`P1` 应该、`P2` 可以延后。
- 每个任务必须标记完成状态：`[x]` 已完成、`[ ]` 未完成。

---

## M0：Tensor 计算最小闭环（可扩展、可演进）

### A. 关键决策（不先定一定返工）
- [ ] (P0) 冻结 `std.tensor` 的 **设备/内存模型**：`Tensor<T>.data` 在 CPU/Metal/CUDA 下如何统一表达（是否需要 `Device/Buffer/TensorHandle` 新类型）；并据此决定 `cai/docs/spec/tensor.md` 是否继续 Frozen 或改 Draft
- [ ] (P0) 冻结 **dtype 扩展方式**：至少支持 `f16/f32/int`，且新增 dtype 不需要重写编译器（不能写死某几个 dtype）
- [ ] (P0) 划定 **核心算子集合**（少但完整，可组合出推理管线）：elementwise(+broadcast)、reduce、matmul、view(reshape/transpose/slice)、layout(contiguous)

### B. 语言与编译器基础（支撑 Tensor，但不为 Tensor 写死）
- [ ] (P0) 语法/语义闭环：覆盖 `struct/enum(payload)/impl/trait/static dispatch/import/extern` 并产出稳定诊断
- [ ] (P0) 泛型单态化（至少覆盖 `Tensor<T>`/`Option<T>`/`Slice<T>`/`Ptr<T>`）
- [ ] (P0) 所有权/借用检查最小可用：`Ref<T>`/`Slice<T>` 权限与冲突检查可用（为后续 alias 优化提供正确性基础）：`cai/docs/spec/ref.md`、`cai/docs/spec/slice.md`、`cai/docs/spec/ownership.md`
- [ ] (P0) 多返回与错误码 lowering 完整实现（含 `? {}` 控制流约束）：`cai/docs/spec/abi.md`、`cai/docs/spec/error.md`、`cai/docs/spec/expressions.md`
- [ ] (P0) MIR 生成：覆盖上述语义信息（借用/错误码/求值顺序/ABI 边界），作为主 IR：`cai/docs/COMPILER.md`

### C. Tensor 语义与标准库 API（先少，但必须“完整定义”）
- [ ] (P0) 补齐并冻结 tensor 的 **shape/stride/view/contiguous/readonly** 语义与错误行为（避免未来改动 ABI 或语义）：`cai/docs/spec/tensor.md`
- [ ] (P0) 冻结并实现 `std.tensor` 最小 API（不依赖特定后端）：创建、view、layout、elementwise、reduce、matmul
- [ ] (P0) 约束并实现“零拷贝 view 变换”：reshape/transpose/slice 必须默认不复制；需要复制时走显式 `contiguous()`

### D. Tensor lowering（只对 Tensor 使用 MLIR）
- [ ] (P0) MIR 中引入 **可识别的 tensor 计算节点**（不要把 tensor 降成普通循环再识别）
- [ ] (P0) 定义并实现 `MIR(tensor) -> MLIR -> LLVM IR` 的 lowering 合同：
  - [ ] 哪些算子进入 MLIR
  - [ ] 哪些算子回退到 runtime 调用（并冻结回退行为）
  - [ ] shape/stride/alias 信息如何携带以支持优化
- [ ] (P0) 确保 MLIR/LLVM 优化不破坏 CAI 语义（求值顺序、错误码、defer）：`cai/docs/spec/performance.md`

### E. 运行时与执行（先 CPU 闭环，但不阻塞 GPU）
- [ ] (P0) 统一 allocator 与 OOM 行为：明确“错误码 vs 运行时错误”的边界并实现一致策略（不得不同模块行为不一致）
- [ ] (P0) 与 async 语义对齐：计算不被抢占、I/O await 不阻塞调度线程：`cai/docs/spec/runtime.md`、`cai/docs/spec/async.md`
- [ ] (P0) 基础并行执行框架（实现可先 CPU 线程池，但接口/抽象不得写死为 CPU）：用于 matmul/reduce 等

### F. 工具链最小可用（为后续“获得感”打底）
- [ ] (P0) MIR dump（Draft 合同也可）：能打印 tensor 节点、shape/stride、借用关系与释放点（便于定位峰值内存来源）
- [ ] (P0) 性能/正确性基线：micro-bench（matmul/elementwise/reduce）+ 基础回归测试集

---

## M1：GPU 后端与跨平台路由（在 M0 之后）
- [ ] (P0) 实现设备抽象的第一个后端（Metal 或 CUDA 二选一）并跑通核心算子
- [ ] (P1) 引入第二个后端（补齐跨平台路由）
- [ ] (P1) 路由可复现：同配置/同环境得到稳定的后端选择与算子实现（便于调试与回放）
- [ ] (P2) 显存预警与插桩采集（先做“可证明上界 + 插桩对比”，不要承诺无条件精确峰值）：`cai/docs/WHITEPAPER.md`

---

## M2+：生态补齐（能用到再做，避免返工）

### A. 包/策略/审计（已冻结规范，等待实现）
- [ ] (P1) 实现 `cai.toml` + `cai.lock` 解析与依赖锁定：`cai/docs/spec/package.md`
- [ ] (P1) 实现 `[policy]` 能力强制与 JSONL 审计事件：`cai/docs/spec/policy.md`

### B. 仍保持 Draft 的规范（先不冻结/不实现）
- [ ] (P2) `std.net`：`cai/docs/spec/std_net.md`
- [ ] (P2) `std.thread`：`cai/docs/spec/std_thread.md`
- [ ] (P2) `std.atomic`：`cai/docs/spec/std_atomic.md`
- [ ] (P2) Windows `std.fs` 路径细节：`cai/docs/spec/std_fs_windows.md`

